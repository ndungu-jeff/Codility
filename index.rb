class String
  # merging overlapping strings
  # split the string and map in to it
  # use the join method to join the array
  # use the capitalize method to loop the array
    
  end
    def camelCase
      split.map(&:capitalize).join
    end
    
  end